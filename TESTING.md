# Verification

Run the public test suite with:

```sh
uv run pytest -q
```

The tests use temporary, synthetic data. No household database, credentials,
network addresses, device names, school names, camera names, or local paths are
included in this repository.
